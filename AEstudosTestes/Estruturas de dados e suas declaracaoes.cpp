#include <bits/stdc++.h>

using namespace std;

int main(int argc, char const *argv[]){
	/*
	vector<tipo de dados> nome da instancia;
	pair<tipo 1, tipo2> nome da instancia;
	map<chave, valor> nome da instancia;
	set<chave> nome instancia => valores unicos e ordenados
	queue(fila)<tipo> nome instancia => so remove o primeiro adicionado
	stack(pilha)<tipo>  nome da instancia => so remove o ultimo adicionado
	
	
	outras formas
	
	pair<pair<int, int>, int> nomeinstancia => acessa:
	
	nomeinstancia.first.first
	nomeinstancia.first.second
	nomeinstancia.second
	*/

	return 0;
}

