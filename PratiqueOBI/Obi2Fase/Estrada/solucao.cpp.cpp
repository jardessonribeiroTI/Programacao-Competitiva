#include <bits/stdc++.h>
#define ALL(x) x.begin(), x.end()
#define modulo(x) (x < 0 ? (x * (-1)) : x)
#define MAXN 100010
#define MAXV 1010
#define ll long long

using namespace std;
int T, N, city[100000];
int main(int argc, char const * argv[]){

    cin>>T>>N;
    for (int i = 1; i <= N; i++)
    {
        cin>>city[i];
    }
    

    for (int i = 0; i < N; i++)
    {
        
    }
    

    return 0;
}